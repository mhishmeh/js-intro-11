const arr1 = []; // created using array literal
const arr2 = new Array() // created using constructor


const names = ['John', 'Jane', 'Alex', 'Max', 'Mariia']

// Find the average of first and last element in the below array
const numbers = [5, 7, 12, 15];


console.log((numbers.at(0) + numbers.at(-1)) / 2)
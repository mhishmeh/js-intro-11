function area_and_perimeter(a,b) {
    let area = a * b;
    let perimeter = 2 * (a+b);

    console.log(`The area of the rectangle is: ${area} and the perimeter is: ${perimeter}`);
};




function salaryCalculator(salary) {
    let yearly = salary
    let monthly = yearly / 12
    let weekly = yearly / 52
    let biweekly = yearly / 26

console.log(`The salary yearly is ${yearly}, monthly is ${monthly}, weekly is ${weekly},and biweekly is ${biweekly}`);
};

salaryCalculator(120000);


for (let index = 0; index < 20; index++) {
    console.log(index)
    
    
}

let side1 = 5;
let side2 = 8;

function area_and_perimeter(a,b) {
    let area = a * b;
    let perimeter = 2 * (a+b);

    console.log(`The area of the rectangle is: ${area} and the perimeter is: ${perimeter}`);
};

area_and_perimeter(side1,side2);

console.log('4' > '3');


console.log(22.245.toFixed(2))